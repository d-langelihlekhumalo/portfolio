# Cloudflare Pages — https://developers.cloudflare.com/pages/configuration/redirects/
#
# Every public route (/, /blog, /blog/:slug) is its own real prerendered
# static file (see scripts/prerender.mjs) — no SPA fallback needed, so a
# nonexistent URL correctly 404s instead of silently serving the homepage.
#
# /admin gets its own client-side-routing fallback rule once it exists.
