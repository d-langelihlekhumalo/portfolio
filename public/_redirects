# Cloudflare Pages — https://developers.cloudflare.com/pages/configuration/redirects/
# SPA fallback: existing static files are served first; everything else gets index.html.
/*    /index.html    200
